import { useState } from "react";
import "./App.css";

function App() {
  const [boardData, setBoardData] = useState(JSON.parse());
  const [characterArray, setCharacterArray] = useState([]);
  return <div></div>;
}

export default App;
